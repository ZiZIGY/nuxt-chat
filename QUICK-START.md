# 🚀 Быстрый старт с Cloudflare Tunnel

## Шаг 1: Установка cloudflared

1. Скачайте: https://github.com/cloudflare/cloudflared/releases/latest
2. Скачайте `cloudflared-windows-amd64.exe`
3. Переименуйте в `cloudflared.exe`
4. Положите в `C:\cloudflared\` (или добавьте в PATH)

## Шаг 2: Запуск

### Простой способ:

Запустите `start-with-tunnel.bat` - он запустит все автоматически!

### Ручной способ:

**Терминал 1:** Запустите приложение
```bash
yarn dev
```

**Терминал 2:** Запустите Socket.io сервер
```bash
yarn dev:socket
```

**Терминал 3:** Запустите туннель для приложения
```bash
cloudflared tunnel --url http://localhost:3000
```
Скопируйте URL (например: `https://abc123.trycloudflare.com`)

**Терминал 4:** Запустите туннель для Socket.io
```bash
cloudflared tunnel --url http://localhost:3001
```
Скопируйте URL (например: `https://xyz789.trycloudflare.com`)

## Шаг 3: Настройка

1. Откройте URL приложения в браузере (из терминала 3)
2. На главной странице в поле "Настройки сервера" введите URL Socket.io (из терминала 4)
3. Создайте комнату или подключитесь к существующей
4. Отправьте URL приложения другу
5. Друг должен также указать URL Socket.io в настройках

## Готово! 🎉

Теперь вы можете общаться через P2P чат из любой точки мира!

## Важно

- Не закрывайте терминалы с туннелями
- При перезапуске cloudflared URL могут измениться
- Оба туннеля должны быть запущены одновременно

