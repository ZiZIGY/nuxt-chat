# 📋 Пошаговая инструкция по развертыванию

## Шаг 1: Установка зависимостей проекта

1. **Убедитесь, что установлены:**

   - Node.js (версия 18 или выше)
   - Yarn (или npm)

2. **Установите зависимости проекта:**
   ```bash
   yarn install
   ```

## Шаг 2: Установка Cloudflare Tunnel (cloudflared)

1. **Скачайте cloudflared:**

   - Перейдите: https://github.com/cloudflare/cloudflared/releases/latest
   - Найдите `cloudflared-windows-amd64.exe`
   - Скачайте файл

2. **Установите cloudflared:**

   - Создайте папку: `C:\cloudflared\`
   - Переместите скачанный файл в эту папку
   - Переименуйте в `cloudflared.exe`

3. **Добавьте в PATH (опционально, но рекомендуется):**

   - Нажмите Win + X → "Система"
   - "Дополнительные параметры системы"
   - "Переменные среды"
   - В "Системные переменные" найдите `Path` → "Изменить"
   - "Создать" → введите `C:\cloudflared`
   - "ОК" везде

4. **Проверьте установку:**
   Откройте новый терминал и выполните:

   ```bash
   cloudflared --version
   ```

   Должна показаться версия (например: `cloudflared version 2025.11.1`)

## Шаг 3: Запуск приложения

### Вариант A: Автоматический запуск (рекомендуется)

1. **Запустите скрипт:**

   ```bash
   start-with-tunnel.bat
   ```

2. **Скрипт автоматически:**

   - Запустит Nuxt приложение (порт 3000)
   - Запустит Socket.io сервер (порт 3001)
   - Запустит два Cloudflare Tunnel

3. **Дождитесь запуска всех сервисов** (около 10-15 секунд)

### Вариант B: Ручной запуск

**Терминал 1 - Nuxt приложение:**

```bash
yarn dev
```

Дождитесь сообщения: `➜ Local: http://localhost:3000/`

**Терминал 2 - Socket.io сервер:**

```bash
yarn dev:socket
```

Дождитесь сообщения: `✅ Socket.io server running on port 3001`

**Терминал 3 - Туннель для приложения:**

```bash
cloudflared tunnel --url http://localhost:3000
```

Или используйте: `start-tunnel-app.bat`

**Терминал 4 - Туннель для Socket.io:**

```bash
cloudflared tunnel --url http://localhost:3001
```

Или используйте: `start-tunnel-socket.bat`

## Шаг 4: Получение URL

После запуска туннелей вы увидите в каждом окне что-то вроде:

```
+--------------------------------------------------------------------------------------------+
|  Your quick Tunnel has been created! Visit it at (it may take some time to be reachable):  |
|  https://abc123.trycloudflare.com                                                          |
+--------------------------------------------------------------------------------------------+
```

**Скопируйте оба URL:**

1. **URL приложения** (из терминала 3 или окна "Cloudflare Tunnel - App")

   - Например: `https://abc123.trycloudflare.com`

2. **URL Socket.io** (из терминала 4 или окна "Cloudflare Tunnel - Socket")
   - Например: `https://xyz789.trycloudflare.com`

## Шаг 5: Настройка приложения

1. **Откройте URL приложения в браузере:**

   - Скопируйте URL приложения (из шага 4)
   - Вставьте в адресную строку браузера
   - Нажмите Enter

2. **Настройте Socket.io сервер:**

   - На главной странице найдите поле "Настройки сервера"
   - Введите URL Socket.io (из шага 4)
   - Например: `https://xyz789.trycloudflare.com`

3. **Создайте комнату:**
   - Нажмите "Создать новую комнату"
   - Или подключитесь к существующей комнате по ID

## Шаг 6: Отправка друзьям

**Отправьте друзьям два URL:**

```
Привет! Подключись к чату:

1. Открой этот сайт: https://abc123.trycloudflare.com

2. В настройках Socket.io сервера (на главной странице) введи:
   https://xyz789.trycloudflare.com

3. Создай комнату или подключись к существующей по ID
```

## Шаг 7: Проверка работы

1. **Проверьте у себя:**

   - Откройте URL приложения
   - Укажите URL Socket.io в настройках
   - Создайте комнату
   - Попробуйте отправить сообщение (для теста откройте в двух вкладках)

2. **Проверьте у друга:**
   - Друг должен открыть URL приложения
   - Указать URL Socket.io в настройках
   - Подключиться к той же комнате
   - Вы должны увидеть друг друга и обмениваться сообщениями

## ⚠️ Важно помнить:

- **Не закрывайте терминалы** с запущенными сервисами и туннелями
- **Все 4 окна должны быть открыты** (или 2 окна при автоматическом запуске)
- При перезапуске cloudflared **URL могут измениться** - нужно будет обновить их
- Для остановки нажмите `Ctrl+C` в каждом терминале

## 🔧 Решение проблем

### Проблема: cloudflared не найден

**Решение:** Убедитесь, что cloudflared.exe находится в `C:\cloudflared\` или добавлен в PATH

### Проблема: Порт уже занят

**Решение:**

```bash
# Проверьте, что запущено на портах
netstat -an | findstr "3000 3001"

# Остановите процессы или измените порты в настройках
```

### Проблема: Туннель не подключается

**Решение:**

- Проверьте, что приложение запущено на localhost:3000
- Проверьте, что Socket.io сервер запущен на localhost:3001
- Перезапустите туннели

### Проблема: Друг не может подключиться

**Решение:**

- Убедитесь, что вы отправили правильные URL
- Проверьте, что оба туннеля запущены
- Попросите друга проверить консоль браузера (F12) на ошибки

## ✅ Готово!

Теперь ваше приложение доступно из интернета, и друзья могут к нему подключиться!
